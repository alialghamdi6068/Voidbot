document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('settings-form');if(!form)return;form.addEventListener('submit',async e=>{e.preventDefault();const result=document.getElementById('result');const payload={};new FormData(form).forEach((v,k)=>{payload[k]=v});['xp_min','xp_max','level_cooldown'].forEach(k=>{if(payload[k]!=='')payload[k]=Number(payload[k])});try{const res=await fetch(`/api/guild/${window.FLAME_GUILD}/settings`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const data=await res.json();result.textContent=data.ok?'✅ تم حفظ الإعدادات بنجاح.':'❌ تعذر حفظ الإعدادات.'}catch{result.textContent='❌ تعذر الاتصال بالسيرفر.'}})});
